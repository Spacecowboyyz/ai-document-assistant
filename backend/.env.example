# Application environment (shown in GET /health)
ENVIRONMENT=development

# Semantic version returned by GET /health
APP_VERSION=1.0.0

# Comma-separated CORS origins; use * to allow all (development only)
CORS_ORIGINS=*

# Uvicorn bind address and port
HOST=0.0.0.0
PORT=8000
